import SwiftUI

@main
struct CardVoiceApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .windowStyle(.titleBar)
        .defaultSize(width: 1120, height: 780)
    }
}
